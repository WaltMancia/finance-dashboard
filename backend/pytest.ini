[pytest]
# Dónde buscar los tests
testpaths = tests

# Modo asyncio para tests asíncronos
asyncio_mode = auto

# Muestra más detalle en los errores
addopts = -v --tb=short

# Ignora warnings de librerías externas
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning